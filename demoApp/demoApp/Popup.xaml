<?xml version="1.0" encoding="UTF-8"?>
<Pages:PopupPage
    xmlns:Pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup" xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="E_WELL.Popup" >
    <StackLayout Margin="12"
                 Padding="24"
                 Spacing="24"
                 BackgroundColor="White"
                 HorizontalOptions="Center"
                 VerticalOptions="Center">
        <StackLayout>
            <Label x:Name="lblTitle" Text=""></Label>
            <Entry x:Name="entSubtitle" FontSize="30" Text=""/>
        </StackLayout>
        <StackLayout Orientation="Horizontal" >
        <Button BackgroundColor="DodgerBlue" FontSize="30" Text="OK" TextColor="White" Margin="10,0,0,0" HorizontalOptions="Start" Clicked="Ok_Cliked"/>
        <Button BackgroundColor="DodgerBlue" FontSize="30" Text="Cancel" TextColor="White" Margin="0,0,10,0" HorizontalOptions="End" Clicked="Cancel_Cliked"/>
        </StackLayout>
    </StackLayout>
</Pages:PopupPage>